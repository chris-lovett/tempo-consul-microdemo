apiVersion: consul.hashicorp.com/v1alpha1
kind: PeeringAcceptor
metadata:
  name: vm-dc
  namespace: consul
spec:
  peer:
    secret:
      name: peering-token-vm-dc
      key: data
      backend: kubernetes
